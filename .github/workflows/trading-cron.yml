# DISABLED — superseded by paper-trading.yml which handles brain + trading + EOD
# Keep this file to avoid breaking any references, but no triggers are active.
name: Trading Cron (disabled)
on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is disabled. Use paper-trading.yml instead.'
        default: 'Use paper-trading.yml'
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use paper-trading.yml workflow instead."
